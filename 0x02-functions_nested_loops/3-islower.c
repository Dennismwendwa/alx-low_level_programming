#include <stdio.h>

/**
  * 3-islower.c:- (int c) - prints letters.
  *
  * Return:- Always 0 (succuss)
  *
  * int c:- - One peremeter int c is required.
  */

int _islower(int c)
{
	int ch;

	if (ch >= 'A' && ch <= 'Z')
	{
		return (0);


	}
	else if (ch >= 'a' && ch <= 'z')
	{
		return (1);


	}

}
