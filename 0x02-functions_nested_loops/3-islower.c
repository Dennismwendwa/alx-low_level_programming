#include <stdio.h>

/**
  * 3-islower.c - checks for lowercase letters.
  *
  * Return:- Always 0 (succuss)
  */

int _islower(int c)
{
	int ch;

	if (ch >= 'A' && ch <= 'Z')
	{
		return 0;


	}
	else if (ch >= 'a' && ch <= 'z')
	{
		return 1;


	}

}
